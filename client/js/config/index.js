'use strict';

import constants from './constants';
import storage from './storage';
import routes from './routes';
import run from './run';

export default {
  constants: constants,
  storage: storage,
  routes: routes,
  run: run
};
